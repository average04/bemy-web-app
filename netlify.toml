# Netlify configuration for Next.js app
[build]
  command = "npm run build"
  publish = "bemy-web-app/.next"

[dev]
  command = "npm run dev"
  targetPort = 3000
  framework = "next"

[[redirects]]
  from = "/api/*"
  to = "/api/:splat"
  status = 200
